/**Area */

var Base = prompt("Ingresar valor de la base del triangulo");
var Altura = prompt("Ingresar valor de la altura del triangulo");

var Base = parseInt(numero1);
var Altura = parseInt(numero2);

var Area = Base * Altura;

document.write("El resultado del Area es " +Area /2 )